#!/bin/bash

heure=$(date +"%H")
min=$(date +"%M")
date=$(date +"%d")"/"$(date +"%m")"/"$(date +%Y)
echo "Bonjour "$USER", bienvenu sur "$HOSTNAME", nous sommes le "$date" et il est "$heure":"$min
PATH=$PATH
